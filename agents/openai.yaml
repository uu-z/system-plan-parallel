interface:
  display_name: "System Plan Parallel"
  short_description: "Think holistically, plan fully, parallelize safely"
  default_prompt: "Use $system-plan-parallel to score this task, produce a full-system plan, and split it into safe independent workstreams."
